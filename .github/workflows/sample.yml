name: shell commands

on: [push]

jobs:
  run-shell-command: 
    runs-on: windows-latest
    if: github.ref == 'refs/heads/master'
    steps:
      - name: echo a string
        run: echo "hello Nidhi!!"
      - name: multiline script
        run: |
          node -v
          npm -v

          

