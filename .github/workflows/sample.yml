name: shell commands

on: [push]

jobs:
  run-shell-command: 
    runs-on: windows-latest
    if: github.ref == 'master'
    steps:
      - name: echo a string
        run: echo "hello world!!"
      - name: multiline script
        run: |
          node -v
          npm -v

          

